type Query {
  info: String!
  users: [User!]!
  user(id: ID!): User
}

type User {
  id: ID!
  name: String!
  age: Int
  tweets: [Tweet!]!
}

type Tweet {
  id: ID!
  body: String!
  author: User!
}

type Mutation {
  createUser(name: String!, age: Int): User!
}
